[project]
name = "SnappyMovement"
version = "1.0"
description = "Makes Borderlands 3 ground movement more responsive by reducing acceleration ramp-up time and stopping inertia without increasing the game's normal movement speed."
authors = [
    { name = "Sol / GPT-5.6 Sol" }
]
maintainers = [
    { name = "Last1SiN" }
]

[project.urls]
Repository = "https://github.com/Last1SiN/SnappyMovement"

[tool.sdkmod]
name = "SnappyMovement"
version = "1.0"
license = { name = "GPL3", url = "https://choosealicense.com/licenses/gpl-3.0/" }
download = "https://github.com/Last1SiN/SnappyMovement/releases/latest/download/SnappyMovement.sdkmod"
coop_support = "Unknown"
supported_games = ["BL3"]
